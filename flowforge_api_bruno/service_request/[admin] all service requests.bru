meta {
  name: [admin] all service requests
  type: http
  seq: 3
}

get {
  url: {{HOST}}/service_request/admin?org_id=1
  body: none
  auth: bearer
}

query {
  org_id: 1
}

auth:bearer {
  token: {{access_token}}
}
