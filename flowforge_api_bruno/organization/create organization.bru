meta {
  name: create organization
  type: http
  seq: 2
}

post {
  url: {{HOST}}/organization
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "name": "test"
  }
}
