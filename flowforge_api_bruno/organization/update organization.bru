meta {
  name: update organization
  type: http
  seq: 4
}

patch {
  url: {{HOST}}/organization
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "org_id": 2,
    "name": "new name"
  }
}
